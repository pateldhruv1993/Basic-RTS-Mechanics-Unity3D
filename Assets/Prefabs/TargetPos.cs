using UnityEngine;
using System.Collections;

public class TargetPos : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
//		if (transform.name == "TempTarget")
//			print ("Target is at "+transform.position);
	}
}
